{% extends '../_layouts/default.njk' %}

{% block content %}

    <div class="container">
        <h1 class="title">{{ product.title }}</h1>
        <div class="flex-center">
            <p>{{ product.content }}</p>
        </div>
    </div>

{% endblock %}
